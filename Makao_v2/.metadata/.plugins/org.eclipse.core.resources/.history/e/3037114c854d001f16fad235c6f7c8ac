package tests;
import java.util.List;

import org.junit.*; 

import client.CardNode;
import client.ClientApplication;
import static org.junit.Assert.* ;
import shared.*;


public class TestDrawingCard {
	
	@Test
	public void testGeneratingCardNodes()
	{
		CardsNodesInitializer initializer = new CardsNodesInitializer();
		initializer.setImagesSource();
		initializer.setSize(30,30);
		List<CardNode> c_nodes = initializer.init();
		//CardNode node= new CardNode(new Card(Card.Name.ACE,Card.Suit.SPADE));
		
	}

}
